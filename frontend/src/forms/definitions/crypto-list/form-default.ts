import type { CryptoFilterValues } from './form-schema';

/** Gelecekteki form default’ları (province form-default.ts ile aynı rol). */
export const defaultFilterValues: CryptoFilterValues = {
  exchange: 'all',
};
